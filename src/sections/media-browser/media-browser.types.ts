export interface NavigateId {
  media_content_id?: string;
  media_content_type?: string;
  title?: string;
}

export type LayoutType = 'auto' | 'grid' | 'list';

export type ViewType = 'favorites' | 'browser';
